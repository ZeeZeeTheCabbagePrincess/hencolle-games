window.HENCOLLE_SPONSOR_ITEM_EFFECTS_SOURCE = {
  medkit: "const { tribute, target, targetPool, fallenIds, events, item, definition, helpers, pickOne } = context;\nconst totalWeight = 1 + 1;\nif (totalWeight <= 0) { return false; }\nlet roll = Math.random() * totalWeight;\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  events.push(`${helpers.formatTributeName(tribute)} uses ${helpers.escapeHtml(definition.label.toLowerCase())} on ${helpers.formatTributeName(target)}, healing their injuries`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  fallenIds.push(target.id);\n  events.push(`${helpers.formatTributeName(tribute)} uses ${helpers.escapeHtml(definition.label.toLowerCase())} on themselves, healing their injuries.`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nreturn false;",
  rifle: "const { tribute, target, targetPool, fallenIds, events, item, definition, helpers, pickOne } = context;\nconst totalWeight = 1;\nif (totalWeight <= 0) { return false; }\nlet roll = Math.random() * totalWeight;\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  fallenIds.push(target.id);\n  events.push(`${helpers.formatTributeName(tribute)} uses ${helpers.escapeHtml(definition.label.toLowerCase())} on ${helpers.formatTributeName(target)} from afar, hitman style, ITS A HEADSHOT!`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nreturn false;",
  explosives: "const { tribute, target, targetPool, fallenIds, events, item, definition, helpers, pickOne } = context;\nconst totalWeight = 1;\nif (totalWeight <= 0) { return false; }\nlet roll = Math.random() * totalWeight;\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  fallenIds.push(target.id);\n  events.push(`${helpers.formatTributeName(tribute)} uses ${helpers.escapeHtml(definition.label.toLowerCase())} on ${helpers.formatTributeName(target)}, blowing them up.`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nreturn false;",
  trapkit: "const { tribute, target, targetPool, fallenIds, events, item, definition, helpers, pickOne } = context;\nconst totalWeight = 1;\nif (totalWeight <= 0) { return false; }\nlet roll = Math.random() * totalWeight;\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  fallenIds.push(target.id);\n  events.push(`${helpers.formatTributeName(tribute)} rigs a ${helpers.escapeHtml(definition.label.toLowerCase())} and catches ${helpers.formatTributeName(target)}, it kills them with spikes.`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nreturn false;",
  sponsorfood: "const { tribute, target, targetPool, fallenIds, events, item, definition, helpers, pickOne } = context;\nconst totalWeight = 1;\nif (totalWeight <= 0) { return false; }\nlet roll = Math.random() * totalWeight;\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  events.push(`${helpers.formatTributeName(tribute)} eats their ${helpers.escapeHtml(definition.label.toLowerCase())} while sitting by the campfire, relaxing.`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nreturn false;",
  dildo: "const { tribute, target, targetPool, fallenIds, events, item, definition, helpers, pickOne } = context;\nconst totalWeight = 1 + 1 + 1;\nif (totalWeight <= 0) { return false; }\nlet roll = Math.random() * totalWeight;\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  fallenIds.push(target.id);\n  events.push(`${helpers.formatTributeName(tribute)} uses ${helpers.escapeHtml(definition.label.toLowerCase())} on ${helpers.formatTributeName(target)}, ${helpers.formatTributeName(target)} passes out after cumming and get eaten by the wolves`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  events.push(`${helpers.formatTributeName(tribute)} uses ${helpers.escapeHtml(definition.label.toLowerCase())} on ${helpers.formatTributeName(target)}, they both get distracted and have a fun time.`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  fallenIds.push(tribute.id);\n  events.push(`${helpers.formatTributeName(tribute)} uses ${helpers.escapeHtml(definition.label.toLowerCase())} while looking at lolis, after hours of gooning they pass out and die from exhaustion!`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nreturn false;",
  sharkbuttplug: "const { tribute, target, targetPool, fallenIds, events, item, definition, helpers, pickOne } = context;\nconst totalWeight = 1 + 1;\nif (totalWeight <= 0) { return false; }\nlet roll = Math.random() * totalWeight;\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  events.push(`${helpers.formatTributeName(tribute)} uses the ${helpers.escapeHtml(definition.label.toLowerCase())} distracting ${helpers.formatTributeName(target)} by cosplaying Gura and swaying their hips`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  events.push(`${helpers.formatTributeName(tribute)} uses the ${helpers.escapeHtml(definition.label.toLowerCase())} and drools over their reflection.`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nreturn false;",
  chrishansen: "const { tribute, target, targetPool, fallenIds, events, item, definition, helpers, pickOne } = context;\nconst totalWeight = 1;\nif (totalWeight <= 0) { return false; }\nlet roll = Math.random() * totalWeight;\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  fallenIds.push(target.id);\n  events.push(`${helpers.formatTributeName(tribute)} operates a sting with ${helpers.escapeHtml(definition.label.toLowerCase())} on ${helpers.formatTributeName(target)}, catching them gooning to lolis, chris hansen pulls out a sniper rifle and shoots ${helpers.formatTributeName(target)} dead`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nreturn false;",
  cuckchair: "const { tribute, target, targetPool, fallenIds, events, item, definition, helpers, pickOne } = context;\nconst totalWeight = 1 + 1;\nif (totalWeight <= 0) { return false; }\nlet roll = Math.random() * totalWeight;\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  fallenIds.push(target.id);\n  events.push(`${helpers.formatTributeName(tribute)} throws the ${helpers.escapeHtml(definition.label.toLowerCase())} at ${helpers.formatTributeName(target)} hitting them in the head and killing them.`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  events.push(`${helpers.formatTributeName(tribute)} sits on the ${helpers.escapeHtml(definition.label.toLowerCase())} while watching ${helpers.formatTributeName(target)} and ${target2 ? helpers.formatTributeName(target2) : 'someone'} fuck.`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nreturn false;",
  sharkhoodie: "const { tribute, target, targetPool, fallenIds, events, item, definition, helpers, pickOne } = context;\nconst totalWeight = 1 + 1;\nif (totalWeight <= 0) { return false; }\nlet roll = Math.random() * totalWeight;\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  events.push(`${helpers.formatTributeName(tribute)} puts on the ${helpers.escapeHtml(definition.label.toLowerCase())} distracting ${helpers.formatTributeName(target)} with the stink!`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  fallenIds.push(tribute.id);\n  events.push(`${helpers.formatTributeName(tribute)} puts on the ${helpers.escapeHtml(definition.label.toLowerCase())} and ${helpers.formatTributeName(target)} pulls the strings choking them.`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nreturn false;",
  powpowgun: "const { tribute, target, targetPool, fallenIds, events, item, definition, helpers, pickOne } = context;\nconst totalWeight = 1 + 1;\nif (totalWeight <= 0) { return false; }\nlet roll = Math.random() * totalWeight;\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  target2 = pickOne(targetPool.filter((candidate) => candidate.id !== target.id)) || null;\n  if (!target2) { return false; }\n  fallenIds.push(target2.id);\n  fallenIds.push(target.id);\n  events.push(`${helpers.formatTributeName(tribute)} uses ${helpers.escapeHtml(definition.label.toLowerCase())} in its rocket launcher form, firing a RPG Round towards ${helpers.formatTributeName(target)} and ${target2 ? helpers.formatTributeName(target2) : 'someone'} killing them both instantly!`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nif ((roll -= 1) <= 0) {\n  let target2 = null;\n  if (!target) { return false; }\n  fallenIds.push(target.id);\n  events.push(`${helpers.formatTributeName(tribute)} shoots ${helpers.formatTributeName(target)} with ${helpers.escapeHtml(definition.label.toLowerCase())} in minigun mode, killing ${helpers.formatTributeName(target)} brutally.`);\n  item.used = Boolean(definition.consumeOnEvent);\n  return true;\n}\nreturn false;"
};
